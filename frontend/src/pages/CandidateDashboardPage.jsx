import { Card } from '../components/common'

export default function CandidateDashboardPage() {
  return (
    <Card>
      <p className="text-body text-ink-secondary">
        Candidate dashboard placeholder — feature pages are added on their own branches.
      </p>
    </Card>
  )
}
